development:
  host: localhost
  database: dummy_development

test:
  host: localhost
  database: dummy_test

# set these environment variables on your prod server
production:
  host: <%= ENV['MONGOMAPPER_HOST'] %>
  port: <%= ENV['MONGOMAPPER_PORT'] %>
  username: <%= ENV['MONGOMAPPER_USERNAME'] %>
  password: <%= ENV['MONGOMAPPER_PASSWORD'] %>
  database: <%= ENV['MONGOMAPPER_DATABASE'] %>
  # slaves:
  #   - host: slave1.local
  #     port: 27018
  #   - host: slave2.local
  #     port: 27019
